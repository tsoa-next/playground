/**
 * Dedicated entrypoint for tsoa-next metadata generation.
 *
 * Controllers are discovered via `controllerPathGlobs` in the root
 * `tsoa.<server>.yaml` files, so this file intentionally stays empty.
 */
export {}
